# 낮은 목소리 / Low Voice — 디자인 의뢰 자료

카페 홈페이지(정적 3페이지)의 현재 상태와, 다듬어주셨으면 하는 부분을 정리한 문서입니다.
코드는 이 문서와 같은 폴더의 파일 전부(`index.html`, `menu.html`, `subscribe.html`, `style.css`, `site.js`)입니다.
빌드 도구·프레임워크·패키지 매니저 없이, HTML 파일을 브라우저에서 바로 열면 동작합니다.

---

## 1. 이 가게가 어떤 곳인지

커피를 못 마시는 사장이 여는 커피숍입니다. 출발점은 이것 하나입니다.

> 음료 때문에 오는 카페가 아니라, **공간** 때문에 오는 카페.

사장이 예전에 자주 가던 카페가 있었는데, 간 이유가 커피가 아니라 **거기 늘 있던 이타적인 주인**과
그 주인이 직접 굴리던 영어 스터디였습니다. 그 경험을 그대로 가게로 옮긴 것이 이 브랜드입니다.

핵심 규칙 세 가지 — 디자인 판단이 갈릴 때 이 순서로 봐주세요.

1. **조용함이 기본값이다.** 사람 목소리는 계속 있지만 톤이 낮은 공간. 화면도 같아야 합니다.
2. **필터가 곧 브랜딩이다.** "4인 이상 안 받습니다"를 숨기지 않고 첫 화면에 적습니다.
3. **사람이 주인공이다.** 원두 산지 스펙보다 사장·단골·스터디 이야기가 먼저입니다.

말투: 담백하게. 느낌표·과장·마케팅 언어("인생 카페", "역대급")를 쓰지 않습니다.

---

## 2. 페이지 구성

| 파일 | 페이지 | 내용 |
|---|---|---|
| `index.html` | 첫 화면 | 히어로 → 시작 이야기 → 단골 소개 → 공간의 분위기 → 안 하는 것 → 대표 메뉴 맛보기 → 푸터 |
| `menu.html` | 메뉴판 | 대표 메뉴 3종(사연 포함) → 추천 조합 3종 → 그 외 메뉴 가격표 → 푸터 |
| `subscribe.html` | 소식받기 | 어떤 소식을 보내는지 → 실제 문장 예시 → 이름·이메일 신청 폼 → 스터디 안내 → 푸터 |
| `style.css` | — | 세 페이지 공통 스타일 전부 (토큰·컴포넌트·다크모드) |
| `site.js` | — | 다크 모드 토글 (세 페이지 공통) |
| `SHOP.md` | — | 원본 브랜드 문서. 카피의 출처입니다 |

내비게이션(상단)과 푸터(하단 저작권 줄 포함)는 세 페이지가 동일합니다.

---

## 3. 디자인 토큰 (`style.css` 최상단)

밝은 화면이 기본이고, 오른쪽 위 🌙 버튼으로 어두운 화면으로 바뀝니다.
어두운 값은 `:root[data-theme="dark"]`에 있습니다. **모든 색은 토큰으로만 씁니다 — 하드코딩된 색 없음.**

| 토큰 | 밝은 화면 | 어두운 화면 | 쓰임 |
|---|---|---|---|
| `--paper` | `#F4EFE7` | `#1A1714` | 기본 배경 |
| `--paper-2` | `#EDE6DB` | `#221E19` | 강조 섹션·푸터 배경 |
| `--card` | `#FBF8F3` | `#241F1A` | 카드 표면 |
| `--ink` | `#2B2621` | `#E9E1D5` | 본문 글자 |
| `--ink-soft` | `#6D625A` | `#ADA294` | 보조 글자 |
| `--ink-faint` | `#9C9086` | `#7C7264` | 라벨·메모 |
| `--line` | `#DCD3C6` | `#38312A` | 1px 구분선 |
| `--coffee` | `#6B4E3D` | `#C7A582` | 포인트 1 — 인용문·가격 |
| `--berry` | `#A9584F` | `#D98C7E` | 포인트 2 — 거절 문구·세트 가격 |
| `--shadow` | `0 14px 28px -14px rgba(43,38,33,.28)` | `0 14px 30px -14px rgba(0,0,0,.7)` | 카드가 떠오를 때 |

의도: 흰색·검정을 쓰지 않고 따뜻한 종이색과 먹색으로 대비를 낮췄습니다.
어두운 화면도 회색이 아니라 **갈색이 도는 어두운 색**입니다 (조명 낮은 공간의 온기).

---

## 4. 타이포그래피

- 서체 하나만 씁니다: **GMarketSans** (Light 300 / Medium 500 / Bold 700)
- `@font-face`로 jsDelivr CDN에서 `.woff`를 불러옵니다 (`style.css` 최상단)
- **400 굵기가 없습니다.** 300과 500으로만 쓰고 있으니 그대로 유지 부탁드립니다
- 폰트 라이선스(재배포·셀프호스팅 조건)는 저희가 확인하지 못했습니다. 확인 필요합니다

| 요소 | 크기 | 굵기 |
|---|---|---|
| 히어로 h1 | `clamp(2.9rem, 9.4vw, 5rem)` | 300 |
| 안쪽 페이지 h1 | `clamp(2.4rem, 7.5vw, 3.8rem)` | 300 |
| h2 | `clamp(1.5rem, 3.9vw, 1.95rem)` | 500 |
| h3 | `1.1rem` | 500 |
| 본문 | `16px` / line-height `1.95` | 300 |
| 섹션 라벨 | `.68rem` / letter-spacing `.3em` / 대문자 | 300 |

본문 줄간격이 1.95로 넉넉한 것은 의도입니다 (천천히 읽히도록).

---

## 5. 레이아웃 규칙

- 콘텐츠 최대 폭 `56rem` (`.wrap`), 좌우 여백 `1.75rem`
- **읽는 글 최대 폭 `34rem`** (`--measure`) — 모든 `<p>`에 적용됩니다
- 섹션 상하 여백 `4.5rem`, 섹션 사이는 1px 실선(`--line`)으로만 나눕니다
- 박스·둥근 모서리를 거의 쓰지 않습니다. 경계는 얇은 선과 여백으로만 만듭니다
- 이미지가 **한 장도 없습니다** (아래 6번 참고)

---

## 6. 지금 비어 있는 것 · 한계

솔직하게 적습니다. 이 부분이 의뢰의 핵심입니다.

- **사진이 없습니다.** 촬영 전이라 여백과 선만으로 버티고 있습니다. 히어로가 특히 허전합니다
- **로고가 글자뿐입니다.** 심볼·워드마크 없음
- **파비콘, OG 이미지 없음.** 링크를 공유하면 아무 미리보기도 안 뜹니다
- **신청 폼은 시연용입니다.** 서버가 없어 실제로 저장되지 않고, 화면에서만 감사 인사가 뜹니다
- **반응형 분기점이 `600px` 하나뿐입니다.** 태블릿 구간이 검증되지 않았습니다
- **접근성 미검증.** 색 대비(WCAG AA), 키보드 이동 순서, 스크린리더 확인을 못 했습니다
- **주소·전화·이메일·SNS 계정은 전부 가상입니다.** 실제 정보로 바꿔야 합니다
  (연남로 12길 8, 02-336-0114, hello@lowvoice.coffee, @lowvoice.coffee — 모두 가짜)
- 메뉴 가격과 영업시간은 실제 의도에 가깝지만 아직 확정 전입니다

---

## 7. 부탁드리고 싶은 것 (우선순위 순)

1. **히어로 아트디렉션** — 사진 없이 갈지, 사진 한 장을 넣을지. 넣는다면 어떤 컷을 찍어야 할지
   (문 열면 제일 먼저 보이는 것이 **바테이블**, 사장은 늘 **바 안쪽**에 서 있습니다)
2. **색 대비 정리** — 지금 일부러 대비를 낮췄는데, 접근성 기준을 통과하면서 이 톤을 지키는 선을 잡아주세요
3. **타이포 스케일 정리** — 한 서체 두 굵기로만 위계를 만들다 보니 애매한 구간이 있습니다
4. **모바일·태블릿 정리** — 특히 메뉴판의 대표 메뉴 3종과 추천 조합 카드
5. **로고·파비콘·OG 이미지**
6. **여백 리듬** — 섹션마다 눈대중으로 잡아서 규칙이 없습니다. 4pt/8pt 같은 기준을 잡아주시면 좋겠습니다

---

## 8. 지켜주셨으면 하는 것

- 흰 배경(`#FFF`)과 순검정(`#000`)을 쓰지 말아주세요. 종이색과 먹색이 브랜드입니다
- 포인트 색은 커피색·딸기색 **둘까지**. 셋째 색이 들어오면 조용함이 깨집니다
- 느낌표, 배지("BEST", "NEW"), 그러데이션, 반짝이는 효과 없이 부탁드립니다
- "4인 이상 안 받습니다" 문구를 작게 숨기지 말아주세요. 이 가게에서는 거절이 곧 소개입니다
- 움직임은 6px 정도 떠오르는 수준까지. 튕기거나 확대되는 효과는 이 가게 톤이 아닙니다

---

## 9. 지금 들어가 있는 인터랙션

넘겨받으실 때 참고하시라고 적습니다. 유지·개선 모두 열려 있습니다.

- **다크 모드** — 🌙 버튼. 선택을 `localStorage`(키: `lowvoice-theme`)에 저장하고,
  `<head>`의 짧은 스크립트가 렌더 전에 먼저 적용해 깜빡임을 막습니다
- **카드 호버** — `translateY(-6px)` + 그림자, `.32s cubic-bezier(.22,.7,.3,1)`
- **신청 완료** — 폼이 사라지고 감사 인사가 뜨며, 캔버스 색종이가 3.4초간 떨어집니다.
  색은 가게 토큰에서 읽어오므로 다크 모드에서도 톤이 맞습니다
- **`prefers-reduced-motion: reduce`** 를 켠 사용자에게는 전환·떠오름·색종이가 모두 꺼집니다

---

## 10. 전달 형식

- 저희 쪽은 **정적 HTML/CSS**입니다. Figma 시안으로 주셔도 되고, 이 파일들을 직접 고쳐주셔도 됩니다
- 고쳐주실 경우 색은 `style.css`의 토큰만 수정해주시면 세 페이지에 한 번에 반영됩니다
- 카피 원본은 `SHOP.md`에 있습니다. 문구를 바꾸실 일이 있으면 그 문서도 같이 봐주세요
- 궁금한 점은 이 문서 기준으로 물어봐주시면 됩니다
